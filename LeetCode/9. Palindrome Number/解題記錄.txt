public class Solution {
    public bool IsPalindrome(int x) {
    int y=0;
    int j=x;
      if(x<0)
        return false;
      else
        while(j!=0)
        {
            y*=10;
            y+=j%10;
            j/=10;
        }
        return x==y;
    }
}

通常數字運算快於文字