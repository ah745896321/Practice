將數字排序後
計算最小值+最大值
此時 若要增加 Sum(最小值+最大值) 只能將最小值 改為次小值(因不存在比最大值更大的數值)
所以當Sum(nums[0]+nums[len-1])>0時
可以循序判斷Sum(nums[1]+nums[len-1])
當Sum(nums[0]+nums[len-1])<0時
可以循序判斷Sum(nums[0]+nums[len-2])
以此限縮可能的範圍

好處: 原本須Loop兩層的 次數 降低

解題: 固定一點，剩餘兩點用此方法限縮

*題目要求不重複的解
因數列排序過
所以主要判斷相鄰Loop沒有相同數字 即可排除重複

節省最後去除重複步驟
